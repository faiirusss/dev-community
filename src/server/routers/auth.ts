import { router } from "../trpc"

/**
 * login and register are handled by Better Auth directly.
 * This router is a placeholder for any future auth-related tRPC procedures
 */
export const authRouter = router({})